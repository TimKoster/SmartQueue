Add this to your .bashrc

alias r='eq -r'
alias q='eq -q'
alias f='eq -f'
alias a='eq -a'

export PATH=/smartqueue/:"$PATH"
eqbatch () {
    ~/smartqueue/venv/bin/python3.11  ~/smartqueue/queue_controller.py eqbatch "$@"
}

eqconfig () {
    ~/smartqueue/venv/bin/python3.11 ~/smartqueue/queue_controller.py eqconfig "$@"
}

eq () {
    ~/smartqueue/venv/bin/python3.11 ~/smartqueue/queue_controller.py eq "$@"
}

eqcancel () {
    ~/smartqueue/venv/bin/python3.11 ~/smartqueue/queue_controller.py eqcancel "$@"
}

eqclear () {
    ~/smartqueue/venv/bin/python3.11 ~/smartqueue/queue_controller.py eqclear "$@"
}

equpdate () {
    ~/smartqueue/venv/bin/python3.11 ~/smartqueue/queue_controller.py equpdate "$@"
}

if [ -d ~/.bash_completion.d ]; then
    for completion in ~/.bash_completion.d/*; do
        source "$completion"
    done
fi

# External queue end